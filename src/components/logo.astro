---
import { Image } from 'astro:assets';

interface Props {
    size: number;
    className?: string;
}

const { size, className }: Props  = Astro.props

---

<Image src="/gosub-logo.svg" alt="logo" width={size} height={size} class={className}/>
